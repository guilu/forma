# FOR-114 UI Spec

No UI — testing/tooling story.

## API surface

N/A — no backend or frontend API surface change. This story only adds
dev-dependency tooling (`jest-axe`/`vitest-axe` + `axe-core`) and test
files under `frontend/src/`.
