// Prettier baseline for the FORMA frontend (FOR-85). Kept close to defaults;
// single quotes and a 100-char width match the existing skeleton style to keep
// baseline formatting churn minimal.
/** @type {import('prettier').Config} */
export default {
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  printWidth: 100,
};
