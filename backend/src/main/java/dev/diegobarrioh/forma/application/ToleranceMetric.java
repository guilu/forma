package dev.diegobarrioh.forma.application;

/** Which figure a {@link ToleranceEntry} is about (ADR-015 slice 4, section 11). */
public enum ToleranceMetric {
  KCAL,
  PROTEIN_G,
  CARBS_G,
  FAT_G
}
