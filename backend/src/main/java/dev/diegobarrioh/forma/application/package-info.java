/**
 * Application layer: use cases, commands, queries and ports.
 *
 * <p>Orchestrates domain behavior and defines the ports that adapters implement. Depends on the
 * domain layer, never on delivery or infrastructure detail. See ADR-001 and {@code
 * .ai/architecture.md}.
 *
 * <p>Empty in the backend skeleton (FOR-80).
 */
package dev.diegobarrioh.forma.application;
