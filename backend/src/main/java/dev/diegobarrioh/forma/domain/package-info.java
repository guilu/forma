/**
 * Domain layer: entities, value objects, domain services and business rules.
 *
 * <p>This package must stay framework-free (no Spring, persistence or provider
 * dependencies) so business rules remain testable in isolation. Dependencies
 * point inward toward this layer. See ADR-001 and {@code .ai/architecture.md}.
 *
 * <p>Empty in the backend skeleton (FOR-80). Product domain behavior is added by
 * later stories.
 */
package dev.diegobarrioh.forma.domain;
